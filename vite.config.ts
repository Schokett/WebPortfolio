import { defineConfig } from 'vite';

// Bei Project Page (USER.github.io/WebPortfolio/) BASE an Repo-Namen anpassen:
export default defineConfig({
  base: '/WebPortfolio/',   // exakt dein Repo-Name inkl. Groß/Klein
});
